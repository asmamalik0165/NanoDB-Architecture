# NanoDB-Architecture
Mini database system built from scratch in C++ with custom data structures (no STL). Graduate systems programming project implementing buffer pool, query parser, indexing and join optimization using TPC-H benchmark.
